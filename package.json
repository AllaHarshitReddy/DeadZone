{
  "name": "sankat-setu",
  "private": true,
  "version": "0.0.1",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@11.25.0",
  "scripts": {
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r --if-present run test"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}