# Copy to .env and adjust for your machine. .env is gitignored -- never
# commit real credentials. These defaults match infra/docker-compose.yml's
# local single-node dev container, not a real deployment.
COUCHDB_PROTOCOL=http
COUCHDB_HOST=localhost
COUCHDB_PORT=5984
COUCHDB_USER=admin
COUCHDB_PASSWORD=changeme
COUCHDB_DB_NAME=deadzone
